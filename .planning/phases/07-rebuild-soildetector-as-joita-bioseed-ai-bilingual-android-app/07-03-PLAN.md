---
phase: 07-rebuild-soildetector-as-joita-bioseed-ai-bilingual-android-app
plan: "03"
type: execute
wave: 3
depends_on: ["07-02"]
files_modified:
  - joita-biosoil-android/scripts/check-locales.sh
  - joita-biosoil-android/scripts/verify-release.sh
  - joita-biosoil-android/README.md
  - joita-biosoil-android/RELEASE-NOTES.md
  - joita-biosoil-android/keystore.properties.example
  - joita-biosoil-android/app/build/outputs/apk/release/joita-biosoil-4.0.0-release.apk
  - joita-biosoil-android/app/build/outputs/bundle/release/joita-biosoil-4.0.0-release.aab
autonomous: true
requirements: []
must_haves:
  truths:
    - "A JOITA-signed release APK and AAB build from source and pass unit, lint, metadata and signature checks."
    - "The APK is CPU-architecture independent, targets API 36/min 23 and requests only approved permissions."
    - "Future updates remain possible through a delivered, backed-up signing key and documented build process."
  artifacts:
    - path: "joita-biosoil-android/app/build/outputs/apk/release/joita-biosoil-4.0.0-release.apk"
      provides: "Direct-install signed Android release"
    - path: "joita-biosoil-android/scripts/verify-release.sh"
      provides: "Reproducible metadata/signature/logo checks"
    - path: "joita-biosoil-android/README.md"
      provides: "Build, install, sensor and signing documentation"
  key_links:
    - from: "app/build.gradle.kts"
      to: "keystore.properties"
      via: "release signingConfig"
      pattern: "signingConfigs"
---

<objective>
Harden, sign, verify and package the JOITA BioSeed AI – Soil Saathi release, then document installation, hardware limits and future updates.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add release signing that preserves future JOITA updateability</name>
  <read_first>
    - joita-biosoil-android/app/build.gradle.kts
    - joita-biosoil-android/.gitignore
    - .planning/phases/07-rebuild-soildetector-as-joita-bioseed-ai-bilingual-android-app/07-RESEARCH.md
  </read_first>
  <action>Create release-signing directory ignored by Git, generate a 4096-bit RSA JOITA BIOSEED AI keystore valid 10,000 days with a high-entropy generated password, alias `joita-biosoil`, and local `keystore.properties` mode 600. Configure release signing only when properties exist, enabling v1/v2/v3 through Android tooling. Add a redacted `keystore.properties.example`; do not print the password in console or commit key/credentials. Copy the credentials into a local `SIGNING-CREDENTIALS.txt` mode 600 with backup warning for the user.</action>
  <acceptance_criteria>
    - Release keystore and credentials exist locally, both are ignored and not in `git ls-files`.
    - App Gradle release signing loads `keystore.properties` and alias `joita-biosoil`.
    - `keytool -list` confirms one JOITA BIOSEED AI entry without exposing passwords.
  </acceptance_criteria>
  <verify><automated>cd joita-biosoil-android &amp;&amp; test -f release-signing/joita-biosoil-release.jks &amp;&amp; test -f release-signing/SIGNING-CREDENTIALS.txt &amp;&amp; test -z "$(git ls-files release-signing)"</automated></verify>
</task>

<task type="auto">
  <name>Task 2: Build all quality gates and create signed APK/AAB</name>
  <read_first>
    - joita-biosoil-android/app/build.gradle.kts
    - joita-biosoil-android/scripts/check-locales.sh
    - .planning/phases/07-rebuild-soildetector-as-joita-bioseed-ai-bilingual-android-app/07-VALIDATION.md
  </read_first>
  <action>Run locale parity, all JVM tests, Android lint, debug assemble, signed release assemble and bundle. Fix build/lint/test failures within Phase 7 scope. Copy/rename final artifacts to stable names `joita-biosoil-4.0.0-release.apk` and `.aab`. Create `scripts/verify-release.sh` that uses apkanalyzer/apksigner/aapt/unzip/shasum to enforce application ID, version, min/target SDK, approved permissions, optional USB/camera, no native `.so`, exact logo hash and valid signature; ensure it exits nonzero on any mismatch.</action>
  <acceptance_criteria>
    - `./gradlew testDebugUnitTest lintDebug assembleDebug assembleRelease bundleRelease` exits 0.
    - Stable-named APK/AAB files exist and APK verification script exits 0.
    - Test results contain zero failures and lint has zero fatal errors.
  </acceptance_criteria>
  <verify><automated>cd joita-biosoil-android &amp;&amp; ./scripts/check-locales.sh &amp;&amp; ./gradlew testDebugUnitTest lintDebug assembleDebug assembleRelease bundleRelease --no-daemon &amp;&amp; ./scripts/verify-release.sh</automated></verify>
</task>

<task type="auto">
  <name>Task 3: Document installation, sensor truth, privacy and future updates</name>
  <read_first>
    - joita-biosoil-android/app/src/main/AndroidManifest.xml
    - joita-biosoil-android/scripts/verify-release.sh
    - .planning/phases/07-rebuild-soildetector-as-joita-bioseed-ai-bilingual-android-app/07-CONTEXT.md
  </read_first>
  <action>Write README with product scope, screenshots/state overview, prerequisites, exact JDK/SDK/Gradle commands, APK installation via Files/adb, OTG support caveat, known device IDs/protocol, offline/manual/sample behavior, English/Hindi switch, permissions/privacy, release verification, signing-key backup and update procedure. State clearly that the new package cannot replace/update the differently signed reference app and that cloud sync is not configured. Add RELEASE-NOTES 4.0.0 with features and hardware/manual UAT checklist. Record artifact SHA-256 values after build.</action>
  <acceptance_criteria>
    - README contains Installation, USB sensor support, Offline behavior, Languages, Permissions, Signing and Future updates sections.
    - README explicitly says USB requires physical OTG support and new APK cannot in-place update the original package.
    - RELEASE-NOTES contains version 4.0.0 and APK/AAB SHA-256 values.
    - Original APK SHA-256 remains unchanged.
  </acceptance_criteria>
  <verify><automated>rg -n 'Installation|USB sensor|Offline|English|Hindi|Permissions|Signing|Future updates|cannot.*update' joita-biosoil-android/README.md &amp;&amp; shasum -a 256 /Users/meenakshi/Downloads/SoilDetector_3.2.0_release_20260628_190347.apk</automated></verify>
</task>

</tasks>

<verification>
Run the full command in Task 2. If an emulator/device is available, run `adb install -r` and launch smoke test; otherwise persist physical USB/camera/GPS and visual checks in HUMAN-UAT.md while automated release checks pass.
</verification>

