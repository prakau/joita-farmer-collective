name: Deploy JOITA FarmAssist

on:
  push:
    branches: [main]
    paths:
      - "farmassist-src/**"
      - "farmassist/**"
      - ".github/workflows/joita-farmassist-pages.yml"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: joita-farmassist-pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: farmassist-src/package-lock.json
      - run: npm install
        working-directory: farmassist-src
      - run: npm run build
        working-directory: farmassist-src
      - uses: actions/upload-pages-artifact@v3
        with:
          path: .

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
