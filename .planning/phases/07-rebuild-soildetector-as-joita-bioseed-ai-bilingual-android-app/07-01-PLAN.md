---
phase: 07-rebuild-soildetector-as-joita-bioseed-ai-bilingual-android-app
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - joita-biosoil-android/settings.gradle.kts
  - joita-biosoil-android/build.gradle.kts
  - joita-biosoil-android/app/build.gradle.kts
  - joita-biosoil-android/app/src/main/AndroidManifest.xml
  - joita-biosoil-android/app/src/main/java/ai/joita/biosoil/model/Models.kt
  - joita-biosoil-android/app/src/main/java/ai/joita/biosoil/data/SoilRepository.kt
  - joita-biosoil-android/app/src/main/java/ai/joita/biosoil/sensor/SoilProbeProtocol.kt
  - joita-biosoil-android/app/src/main/java/ai/joita/biosoil/sensor/UsbSoilSensorManager.kt
  - joita-biosoil-android/app/src/test/java/ai/joita/biosoil/sensor/SoilProbeProtocolTest.kt
autonomous: true
requirements: []
must_haves:
  truths:
    - "A clean Android project builds with package ai.joita.biosoil, version 4.0.0, compile/target 36 and min 23."
    - "Offline models persist farmer, field and soil-test records while preserving USB, manual and sample source labels."
    - "The optional USB manager speaks the recovered 9600/8N1 protocol without blocking installation on non-OTG phones."
  artifacts:
    - path: "joita-biosoil-android/app/build.gradle.kts"
      provides: "Pinned Android/Compose/USB build configuration"
    - path: "joita-biosoil-android/app/src/main/java/ai/joita/biosoil/sensor/SoilProbeProtocol.kt"
      provides: "Testable poll command and defensive 19-byte parser"
    - path: "joita-biosoil-android/app/src/main/java/ai/joita/biosoil/data/SoilRepository.kt"
      provides: "SQLite offline repository"
  key_links:
    - from: "UsbSoilSensorManager.kt"
      to: "SoilProbeProtocol.kt"
      via: "pollCommand and append/parse"
      pattern: "SoilProbeProtocol"
---

<objective>
Create the maintainable Android foundation, offline domain storage and optional USB soil-probe integration in a new `joita-biosoil-android` folder. Preserve the reference APK unchanged.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Bootstrap pinned API-36 Android project and brand resources</name>
  <read_first>
    - .planning/phases/07-rebuild-soildetector-as-joita-bioseed-ai-bilingual-android-app/07-CONTEXT.md
    - .planning/phases/07-rebuild-soildetector-as-joita-bioseed-ai-bilingual-android-app/07-RESEARCH.md
    - .planning/phases/07-rebuild-soildetector-as-joita-bioseed-ai-bilingual-android-app/07-UI-SPEC.md
    - .gitignore
  </read_first>
  <action>Create `joita-biosoil-android` with Gradle wrapper 8.13, AGP 8.11.1, Kotlin/Compose plugins 2.2.10, Compose BOM 2026.06.00, `activity-compose:1.12.3`, `appcompat:1.7.1`, `core-ktx:1.17.0`, JUnit 4.13.2 and USB serial 3.10.0 through JitPack. Configure application ID/namespace `ai.joita.biosoil`, versionCode 40000, versionName `4.0.0`, compile/target 36, min 23, Java/Kotlin 17, release minification/resource shrinking, and BuildConfig. Add manifest with optional USB host and camera features, coarse/fine location and network-state permissions only, exported launcher MainActivity, FileProvider, Android locale config, and USB device attach filter. Copy the exact logo to `drawable-nodpi/joita_bioseed_logo.png` and create safe adaptive vector launcher assets without changing the full logo. Add local/signing outputs to `.gitignore`; do not add legacy storage, direct-call, background-location or broad package permissions.</action>
  <acceptance_criteria>
    - `joita-biosoil-android/app/build.gradle.kts` contains `applicationId = "ai.joita.biosoil"`, `compileSdk = 36`, `minSdk = 23`, `targetSdk = 36`, `versionName = "4.0.0"`, and USB version `3.10.0`.
    - Manifest declares USB host and camera with `android:required="false"` and contains no READ/WRITE_EXTERNAL_STORAGE.
    - Logo SHA-256 is `aaa21d2752a5e434c0101a3fb65202c42525f855fe6ef441ca315d8b0f34125b`.
    - `./gradlew tasks --no-daemon` exits 0 using JDK 17.
  </acceptance_criteria>
  <verify><automated>cd joita-biosoil-android &amp;&amp; ./gradlew tasks --no-daemon</automated></verify>
</task>

<task type="auto">
  <name>Task 2: Implement typed domain models, advisory engine and SQLite repository</name>
  <read_first>
    - joita-biosoil-android/app/build.gradle.kts
    - .planning/phases/07-rebuild-soildetector-as-joita-bioseed-ai-bilingual-android-app/07-RESEARCH.md
    - .planning/phases/07-rebuild-soildetector-as-joita-bioseed-ai-bilingual-android-app/07-UI-SPEC.md
  </read_first>
  <action>Create `Models.kt` with `ReadingSource { USB, MANUAL, SAMPLE }`, farmer, field, sensor-reading, soil-test, evidence/trust, sync-status and advisory result types. Create `SoilAdvisor.kt` returning a clamped 0–100 transparent advisory score, parameter statuses, conservative next actions and mandatory disclaimer resource key. Create `SoilDatabaseHelper`/`SoilRepository` using SQLite schema version 1 with farmers, fields, soil_tests and settings/sync queue; source is a NOT NULL persisted value, sample data stays labeled SAMPLE, and no government-ID column exists. Seed one visibly sample Indian farmer/field/test only on explicit demo action, not database creation.</action>
  <acceptance_criteria>
    - Models define exactly USB, MANUAL and SAMPLE reading sources.
    - SQLite creation SQL includes farmers, fields and soil_tests and a non-null source column.
    - No source file contains Aadhaar, PAN, government_id, or a fake remote-sync success assignment.
    - `SoilAdvisorTest` proves score is always 0–100 and advisory disclaimer is returned.
  </acceptance_criteria>
  <verify><automated>cd joita-biosoil-android &amp;&amp; ./gradlew testDebugUnitTest --tests '*SoilAdvisorTest' --no-daemon</automated></verify>
</task>

<task type="auto">
  <name>Task 3: Implement recovered USB protocol and permission-safe sensor manager</name>
  <read_first>
    - joita-biosoil-android/app/src/main/java/ai/joita/biosoil/model/Models.kt
    - .planning/phases/07-rebuild-soildetector-as-joita-bioseed-ai-bilingual-android-app/07-RESEARCH.md
    - /tmp/joita-soildetector-audit.CmlJWj/jadx/sources/com/dacundianzi/tr5z/i.java
    - /tmp/joita-soildetector-audit.CmlJWj/apktool/res/xml/device_filter.xml
  </read_first>
  <action>Create `SoilProbeProtocol` with `pollCommand(nonce)` producing `00 04 00 00 00 08 F0 nonce`, big-endian unsigned helpers, true signed 16-bit temperature (do not copy the reference's raw-127-to-zero bug), minimum 19-byte frame parsing, broad range validation and an accumulating buffer for split reads. Create `UsbSoilSensorManager` with states Unsupported, NoDevice, PermissionRequired, Connecting, Connected, Reading, PartialData, Error; custom probe table for vendor 6790 products 29987/21795/21778 plus default prober; explicit immutable USB permission PendingIntent; 9600/8N1; one-second polling; attach/detach cleanup; callbacks on main thread; and source USB for every live record. Never generate sample values on connection failure.</action>
  <acceptance_criteria>
    - Protocol command has eight bytes and parser maps moisture 3–4, signed temperature 5–6, EC 7–8, pH 9–10, N 11–12, P 13–14, K 15–16, fertility 17–18.
    - Tests cover short frame, split buffer, negative temperature, raw 0x7F, valid frame and invalid pH.
    - Manager contains `setParameters(9600, 8, 1, 0)` and the three known product IDs.
    - USB is runtime-optional and manager has an Unsupported state.
  </acceptance_criteria>
  <verify><automated>cd joita-biosoil-android &amp;&amp; ./gradlew testDebugUnitTest --tests '*SoilProbeProtocolTest' --no-daemon</automated></verify>
</task>

</tasks>

<verification>
Run `cd joita-biosoil-android && ./gradlew testDebugUnitTest lintDebug assembleDebug --no-daemon`. Confirm the reference APK SHA-256 remains `3c2def88a20457ddc5ab0c8274337d0ca2bc2a7d6470e2c79dcb05a2eedc9e63`.
</verification>

