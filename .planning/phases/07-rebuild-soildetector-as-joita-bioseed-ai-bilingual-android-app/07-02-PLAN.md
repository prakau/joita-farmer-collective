---
phase: 07-rebuild-soildetector-as-joita-bioseed-ai-bilingual-android-app
plan: "02"
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - joita-biosoil-android/app/src/main/java/ai/joita/biosoil/MainActivity.kt
  - joita-biosoil-android/app/src/main/java/ai/joita/biosoil/ui/JoitaSoilApp.kt
  - joita-biosoil-android/app/src/main/java/ai/joita/biosoil/ui/Screens.kt
  - joita-biosoil-android/app/src/main/java/ai/joita/biosoil/ui/Theme.kt
  - joita-biosoil-android/app/src/main/java/ai/joita/biosoil/mobile/MobileServices.kt
  - joita-biosoil-android/app/src/main/java/ai/joita/biosoil/report/ReportService.kt
  - joita-biosoil-android/app/src/main/res/values/strings.xml
  - joita-biosoil-android/app/src/main/res/values-hi/strings.xml
autonomous: true
requirements: []
must_haves:
  truths:
    - "Users can complete farmer, field, USB/manual/sample test, result, history and report workflows offline."
    - "Every visible workflow is usable in English and Hindi with explicit source/connectivity/evidence states."
    - "GPS, camera, phone, WhatsApp and share handoffs are permission-safe and have fallbacks."
  artifacts:
    - path: "joita-biosoil-android/app/src/main/java/ai/joita/biosoil/ui/JoitaSoilApp.kt"
      provides: "Complete Compose navigation and state"
    - path: "joita-biosoil-android/app/src/main/res/values-hi/strings.xml"
      provides: "Hindi product copy"
    - path: "joita-biosoil-android/app/src/main/java/ai/joita/biosoil/report/ReportService.kt"
      provides: "Offline PDF and share report"
  key_links:
    - from: "JoitaSoilApp.kt"
      to: "SoilRepository.kt"
      via: "offline list/create/update actions"
      pattern: "SoilRepository"
    - from: "Screens.kt"
      to: "UsbSoilSensorManager.kt"
      via: "sensor source and live states"
      pattern: "UsbSoilSensorManager"
---

<objective>
Build the completely new JOITA India-first bilingual interface and connect every screen to offline storage, the USB probe, GPS, camera, report and explicit Android handoffs.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Implement JOITA theme, exact bilingual resources and locale switching</name>
  <read_first>
    - .planning/phases/07-rebuild-soildetector-as-joita-bioseed-ai-bilingual-android-app/07-UI-SPEC.md
    - joita-biosoil-android/app/src/main/AndroidManifest.xml
    - joita-biosoil-android/app/build.gradle.kts
  </read_first>
  <action>Implement Material 3 theme tokens exactly from UI-SPEC: green `#238B2A`, dark `#125C2A`, cream `#FFF9ED`, surface white, turmeric `#F4A622`, soil `#7A4E2D`, error `#BA1A1A`, info `#1769AA`; four type sizes 14/16/20/28sp and two weights 400/600; declared spacing/shape rules; dark theme disabled. Create complete English base and Hindi `values-hi` string resources for all screens/states/errors/disclaimer/accessibility descriptions, `locales_config.xml` listing only `en` and `hi`, and runtime switch with `AppCompatDelegate.setApplicationLocales`. Add `scripts/check-locales.sh` that compares translatable key sets and rejects other locale resource folders.</action>
  <acceptance_criteria>
    - Theme contains all locked hex values and no dynamic color override.
    - English and Hindi resources contain `app_name`, `start_soil_test`, `connect_usb_sensor`, `enter_manually`, `try_sample_reading`, `advisory_disclaimer` and matching key sets.
    - Locale config contains only en and hi.
    - `scripts/check-locales.sh` exits 0.
  </acceptance_criteria>
  <verify><automated>cd joita-biosoil-android &amp;&amp; ./scripts/check-locales.sh &amp;&amp; ./gradlew lintDebug --no-daemon</automated></verify>
</task>

<task type="auto">
  <name>Task 2: Build top-level navigation, Home and farmer/field workflows</name>
  <read_first>
    - joita-biosoil-android/app/src/main/java/ai/joita/biosoil/data/SoilRepository.kt
    - joita-biosoil-android/app/src/main/java/ai/joita/biosoil/ui/Theme.kt
    - .planning/phases/07-rebuild-soildetector-as-joita-bioseed-ai-bilingual-android-app/07-UI-SPEC.md
  </read_first>
  <action>Create `MainActivity` as `AppCompatActivity` hosting `JoitaSoilApp`. Implement Splash/first language, 72dp four-item bottom navigation, Home readiness chips (online/offline, GPS, sensor), hero Start soil test, resume draft, recent result and local-only sync warning. Implement Fields list/search/empty and add/edit form for farmer name, optional mobile, village, district, state, pincode, Khasra/plot, area acre/hectare, crop. Enforce 48dp targets, responsive one/two-column metric/layout rules, 200% font tolerance and persistent saveable/repository state. About/Splash render the exact logo at original aspect ratio and show Made by JOITA BIOSEED AI bilingual attribution.</action>
  <acceptance_criteria>
    - Bottom destinations are Home, Fields, Tests, More via localized resources.
    - Home visibly renders network, GPS and sensor states and local-only sync truth.
    - Field form contains every India-specific field from UI-SPEC and no government-ID input.
    - Full logo uses Fit scaling and publisher attribution is present.
    - `./gradlew assembleDebug lintDebug` exits 0.
  </acceptance_criteria>
  <verify><automated>cd joita-biosoil-android &amp;&amp; ./gradlew assembleDebug lintDebug --no-daemon</automated></verify>
</task>

<task type="auto">
  <name>Task 3: Build source chooser, sensor/manual/sample wizard, evidence and result</name>
  <read_first>
    - joita-biosoil-android/app/src/main/java/ai/joita/biosoil/sensor/UsbSoilSensorManager.kt
    - joita-biosoil-android/app/src/main/java/ai/joita/biosoil/model/Models.kt
    - joita-biosoil-android/app/src/main/java/ai/joita/biosoil/data/SoilRepository.kt
    - .planning/phases/07-rebuild-soildetector-as-joita-bioseed-ai-bilingual-android-app/07-UI-SPEC.md
  </read_first>
  <action>Implement the resumable wizard with source chooser cards Connect USB sensor, Enter manually, Try sample reading. Live screen shows eight `—` metrics until one complete payload; every state and retry/permission action from UI-SPEC is visible; non-OTG phones can choose manual/sample. Manual entry uses numeric keyboards, units and source-note rules. Sample readings show permanent SAMPLE badge. Evidence step uses location/camera launchers with rationale, accuracy/timestamp, denied/settings/manual fallbacks and trust status. Review separates observed/context/missing evidence. Result shows 0–100 advisory score, parameter range/status, advice groups and mandatory bilingual disclaimer. Save writes repository with immutable source badge.</action>
  <acceptance_criteria>
    - Source chooser has exactly USB, manual and sample choices with permanent source labels.
    - Live metrics remain absent/dash before a complete parsed payload.
    - Location/camera are optional for saving but missing evidence changes trust status.
    - Result disclaimer appears in both language resources and code cannot omit it.
    - Offline save/read unit tests and full debug build pass.
  </acceptance_criteria>
  <verify><automated>cd joita-biosoil-android &amp;&amp; ./gradlew testDebugUnitTest lintDebug assembleDebug --no-daemon</automated></verify>
</task>

<task type="auto">
  <name>Task 4: Implement history, PDF/share, guidance, settings and honest sync queue</name>
  <read_first>
    - joita-biosoil-android/app/src/main/java/ai/joita/biosoil/data/SoilRepository.kt
    - joita-biosoil-android/app/src/main/java/ai/joita/biosoil/ui/Screens.kt
    - .planning/phases/07-rebuild-soildetector-as-joita-bioseed-ai-bilingual-android-app/07-UI-SPEC.md
  </read_first>
  <action>Implement Tests history filters and detail/report preview. Create `ReportService` using `PdfDocument` with logo, local report ID, farmer/field, timestamp, GPS/evidence trust, all measurements, source badge, score/advice and disclaimer; share through FileProvider. Add ACTION_DIAL support and WhatsApp ACTION_VIEW with package availability fallback to share/copy; never call/send automatically. More includes local sync queue (Ready/Failed/Local only; no fake Synced), bilingual OTG/probe/pH/NPK/EC/sampling guidance, language/unit/text-size settings, data export/clear demo confirmation, privacy/terms and About.</action>
  <acceptance_criteria>
    - PDF contains source, disclaimer, logo and local report ID.
    - Phone uses ACTION_DIAL; WhatsApp path has a package-missing fallback.
    - Sync UI contains exact `Local only — cloud sync is not configured` resource and no code marks a remote success.
    - Guidance and settings are reachable in both locales.
    - Full Gradle unit/lint/debug build exits 0.
  </acceptance_criteria>
  <verify><automated>cd joita-biosoil-android &amp;&amp; ./gradlew testDebugUnitTest lintDebug assembleDebug --no-daemon</automated></verify>
</task>

</tasks>

<verification>
Run locale parity and full debug checks. Inspect Compose code for every required Home/Fields/Tests/More and wizard screen/state. Manual hardware validation is deferred to Plan 03/UAT because no probe is attached to the build machine.
</verification>

